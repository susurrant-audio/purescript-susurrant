module Test.ShowGauss where

import Prelude
import Test.Spec (describe, it, pending)
import Test.Spec.Assertions (shouldEqual)

showGaussSpec = do
  describe "ShowGauss" do
    pending "is a component that displays a MultivariateGaussian"
